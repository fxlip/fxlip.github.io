---
layout: page
title: 404
permalink: /404.html
hide_footer: true
---

<style>
  /* --- CONFIGURAÇÕES DE TERMINAL --- */
  .terminal-window {
    font-family: Menlo, Monaco, Consolas, "Courier New", monospace;
    color: var(--text-color);
    margin-top: 30px;
    font-size: 14px;
    width: 100%;
    overflow-x: auto;
    scrollbar-width: none;
  }
  .terminal-window::-webkit-scrollbar { display: none; }
  .t-row { display: block; line-height: 1.5; white-space: nowrap; }

  /* --- PALETA DE CORES DRACULA --- */
  .p-user { color: #FF79C6; font-weight: bold; } 
  .p-at, .p-sign { color: var(--base-color); opacity: 0.7; }
  .p-host { color: #BD93F9; font-weight: bold; } 
  .p-path { color: #D8B4FE; font-style: italic; } 
  
  /* Cores específicas */
  .cmd-input { color: #F1FA8C; } 
  .sys-msg { color: #6272A4; font-style: italic; } 
  .err-msg { color: #FF5555; font-weight: bold; } 

  /* Link estilizado */
  .nav-link {
    color: #F1FA8C; 
    text-decoration: none !important;
    border-bottom: 1px solid transparent;
    transition: border-color 0.2s;
  }
  .nav-link:hover {
    border-bottom: 1px solid #F1FA8C;
    cursor: pointer;
  }

  .cursor {
    display: inline-block;
    width: 9px;
    height: 16px;
    background-color: var(--text-color);
    animation: blink 1s infinite;
    vertical-align: sub;
    margin-left: 5px;
    opacity: 0.7;
  }

  @keyframes blink {
    0%, 100% { opacity: 0.7; }
    50% { opacity: 0; }
  }
</style>

<div class="terminal-window">
  
  <div class="t-row">
    <span class="p-user">root</span><span class="p-at">@</span><span class="p-host">fxlip</span>:<span class="p-path">~</span><span class="p-sign">$</span> <span class="cmd-input" id="cmd-display">cd ...</span>
  </div>

  <div class="t-row">
    <span class="sys-msg">resolving pointer... done.</span>
  </div>

  <div class="t-row">
    <span class="err-msg">[FATAL] SEGMENTATION FAULT. Core dumped.</span>
  </div>
  <div class="t-row">
    <span class="err-msg">Error 404: The sector you are looking for is corrupted or does not exist.</span>
  </div>

  <br>

  <div class="t-row">
    <span class="p-user">root</span><span class="p-at">@</span><span class="p-host">fxlip</span>:<span class="p-path">~</span><span class="p-sign">$</span> <a href="/" class="nav-link">cd /home</a><span class="cursor"></span>
  </div>

</div>

<script>
  // EXECUÇÃO CLIENT-SIDE
  // Captura o caminho atual (ex: /linux/pasta-errada)
  const currentPath = window.location.pathname;
  
  // Injeta no terminal
  const cmdElement = document.getElementById('cmd-display');
  if (cmdElement) {
    // textContent previne XSS caso alguém tente injetar HTML na URL
    cmdElement.textContent = "access " + currentPath;
  }
</script>