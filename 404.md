---
layout: page
title: 404
permalink: /404.html
hide_footer: true
---

<style>
  /* --- CONFIGURAÇÕES DE TERMINAL --- */
  .terminal-window {
    font-family: Menlo, Monaco, Consolas, "Courier New", monospace;
    color: var(--text-color);
    margin-top: 0px; 
    font-size: 14px;
    width: 100%;
    overflow-x: auto;
    scrollbar-width: none;
  }
  .terminal-window::-webkit-scrollbar { display: none; }
  .t-row { display: block; line-height: 1.5; white-space: nowrap; }

  /* --- PALETA DRACULA --- */
  .p-user { color: #FF79C6; font-weight: bold; } 
  .p-at, .p-sign { color: var(--base-color); opacity: 0.7; }
  .p-host { color: #BD93F9; font-weight: bold; } 
  .p-path { color: #D8B4FE; font-style: italic; } 
  
  /* Cores Unificadas #878BA6 */
  .cmd-input { color: #878BA6; } 
  
  .sys-response { 
    color: #878BA6; 
    opacity: 1; 
  } 

  /* LINK INTERATIVO (cd /home) */
  .nav-link {
    color: #878BA6; 
    font-weight: normal;
    text-decoration: none !important;
    border: none !important;
    /* Animação contínua e eterna */
    animation: neon-pulse 1.5s infinite alternate ease-in-out;
  }
  
  /* Hover: Apenas aumenta o brilho, NÃO PARA a animação */
  .nav-link:hover {
    text-shadow: 0 0 10px rgba(255, 121, 198, 0.9);
    cursor: pointer;
  }

  .cursor {
    display: inline-block;
    width: 9px;
    height: 16px;
    background-color: var(--text-color);
    animation: blink 1s infinite;
    vertical-align: sub;
    margin-left: 5px;
    opacity: 0.7;
  }

  @keyframes blink {
    0%, 100% { opacity: 0.7; }
    50% { opacity: 0; }
  }

  /* Animação: Cinza -> Rosa -> Cinza */
  @keyframes neon-pulse {
    from { 
      color: #878BA6; 
      text-shadow: none; 
    }
    to { 
      color: #FF79C6; 
      text-shadow: 0 0 5px rgba(255, 121, 198, 0.6); 
    }
  }
</style>

<div class="terminal-window">
  
  <div class="t-row">
    <span class="p-user">root</span><span class="p-at">@</span><span class="p-host">fxlip</span>:<span class="p-path">~</span><span class="p-sign">$</span> <span class="cmd-input" id="cmd-display">cd ...</span>
  </div>

  <div class="t-row">
    <span class="sys-response" id="err-display">bash: cd: ...: Arquivo ou diretório inexistente</span>
  </div>
  
  <br>

  <div class="t-row">
    <span class="p-user">root</span><span class="p-at">@</span><span class="p-host">fxlip</span>:<span class="p-path">~</span><span class="p-sign">$</span> <a href="/" class="nav-link">cd /home</a><span class="cursor"></span>
  </div>

</div>

<script>
  // Captura a URL atual
  const currentPath = window.location.pathname;
  
  // Elementos do DOM
  const cmdElement = document.getElementById('cmd-display');
  const errElement = document.getElementById('err-display');
  
  // Formata o caminho (corta se for muito longo)
  const displayPath = currentPath.length > 40 ? currentPath.substring(0, 37) + "..." : currentPath;

  if (cmdElement) {
    cmdElement.textContent = "cd " + displayPath;
  }

  if (errElement) {
    // Réplica exata do bash
    errElement.textContent = "bash: cd: " + displayPath + ": Arquivo ou diretório inexistente";
  }
</script>